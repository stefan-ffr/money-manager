# Domain für Services
DOMAIN=example.com

# Let's Encrypt Email
ACME_EMAIL=admin@example.com

# Traefik Dashboard Auth (generiert mit: htpasswd -nb admin <password>)
# Beispiel: admin / changeme
TRAEFIK_AUTH=admin:$$apr1$$...

# Log Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
